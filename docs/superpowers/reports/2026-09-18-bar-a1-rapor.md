# Bar A1 — Uygulama ve İzole Test Raporu (Aşama 2 öncesi)

**Tarih:** 2026-09-18 · **Dal:** `bar-a1` (worktree `C:\Users\USER\Projects\gurok-bar-a1`)
**Taban:** `origin/main` 9c06661 dalın atası (stok tarih düzeltmesi ve denetleyici CRLF düzeltmesi dahil).
**Üretim:** Hiçbir şey uygulanmadı. Push, deploy, canlı migration **yok**. Aşama 2'ye geçilmedi.

---

## 1. Kullanıcı kararları → uygulama

| Karar | Nasıl karşılandı | Test |
|---|---|---|
| **1** Ücretli siparişte personel açık oda/misafir doğrulaması zorunlu; doğrulayan kaydedilir | Ücretli sipariş her kanalda `oda_dogrulama_durumu='bekliyor'` başlar; hazırlık `ODA_DOGRULAMASI_BEKLIYOR` ile kapalı. `bar_siparis_oda_dogrula(id, beyan)` yalnız oturum açmış personel, açık beyanla; `dogrulayan`, zaman, beyan, folyo yazılır. `bar_siparis_oda_reddet` nedenli. Garson ekranında sipariş sonrası ayrı beyan paneli; kuyrukta Doğrula/Reddet. | DB A4–A7d; ekran K1–K5, G2–G4; Edge H8–H9 |
| **2** İptal/kapalı folyo: kullanılan miktar kaydedilir, otomatik zayi değil, neden ayrı; "servis edildi" istisnası yalnız yetkili açar; ikinci düşüm/borç yok; istisna çözülmeden tamamlanmış sayılmaz | Hazırlanmış siparişte iptal, her bileşen için kullanılan miktarı ister (varsayılan yok); kullanım `bar_stok_tuketimleri.tur='iptal_kullanimi'` + `kullanim_nedeni` + açıklama. Kapalı folyoda `FOLYO_KAPALI`; `p_fiziksel_servis_beyani` yalnız `bar_siparis_yonetimi=tam`; stok bir kez düşer, borç yazılmaz, sipariş `istisna_bekliyor`; çözüm `bar_borc_istisnasi_coz` (ön büro). Aynı siparişe ikinci teslim `zaten_*`. | DB B1–B7, C1–C3; ekran K6–K15 |
| **3** Sayım: gözlem saklanır; rezervasyonla çelişen düzeltme bekler; uygulanırken aradaki hareketler korunur; kısmi uygulama açık görünür | `stok_sayim_onayla` tek sunucu işlemi: fark onay anındaki güncel stoğa **delta** olarak eklenir; çelişen eksi düzeltme `stok_sayim_bekleyenleri`'ne gider. `stok_sayim_bekleyen_uygula` o anki stoğa farkı ekler (sayılan miktar üzerine yazılmaz). Ekranda kısmi uygulama açık uyarı + bekleyen listesi (Uygula/İptal). Eksik kaydedilmiş oturum `SAYIM_EKSIK`. | DB E1–E6b; ekran S1–S4 |
| **4** Tüm barları kapsayan yayın adayı; Edge E2E geçmeden yayın yok | Migration tüm barlar için; Edge E2E izole ortamda **22/22** geçti (bölüm 3). Yayın ayrıca `CANLIYA UYGULA` bekler. | Edge A+B |

**Benim yorumlarım (kullanıcı kararı DEĞİL — onay bekler):** Ö11 iptal kullanımı stoktan
düşer (zayi sayılmaz ama tüketimdir); neden listesi `hazirlandi_servis_edilmedi / dokuldu_kirildi /
misafir_iade / diger`. Ö12 istisna açma `bar_siparis_yonetimi=tam`, çözme `pms_folio≥kayit`.
Ö13 durum adı `istisna_bekliyor`. Ö14 sayım onayı `stok_takip≥kayit`; Ö15 bekleyen
uygula/iptal `stok_takip=tam`. Kısmi uygulama uyarısı `alert` ile (engelleyici) gösterilir.

## 2. Test sonuçları (hepsi izole; üretime bağlanmaz)

| Takım | Sonuç | Ne ölçer |
|---|---|---|
| `bar-test-taban` | 9/9 | Taban = 09-13 dökümü + 2 migration; stok RPC md5 = üretim ölçümü |
| `bar-a1-once` | 9/9 | Dokuz kusur **üretim kodunda** var (ör. eşzamanlı çift teslim stok 10→6) |
| `bar-a1-guvenlik` | 54/54 | A1 veritabanı davranışı, kararlar 1–3 |
| `bar-a1-negatif` | 5/5 | Kritik korumalar kaldırılınca testler düşüyor |
| `stok-guncelleme-tarihi-bar-sira` | 16/16 | Tarih düzeltmesi → A1 → geri alma; geri alma sonrası 8 fonksiyonun gövde + ayar + **etkin yetkisi** A1 öncesiyle birebir (yeni 4b2) + N1–N4 |
| `bar-a1-ekran` | 26/26 | Ekranların gerçek betiği, gerçek PostgREST üzerinden |
| `bar-edge-e2e` | 22/22 | Bölüm 3 |
| `stok-ekran` / `stok-veri-eksiksizlik` / `stok-guncelleme-tarihi` | 21/21 · 33/33 · 14/14 | Regresyon |
| Denetleyici | 23 dosya 0 HATA 0 UYARI; birim testleri 15/15 | A1 + geri alma dahil |

Geri alma dosyası Git'in çıkaracağı CRLF halde de 16/16 (psql satır sonunu normalize ediyor).
Son toplu koşuda `bar-a1-negatif` CRLF'li çalışma kopyasında **kırıldı** (çok satırlı arama metni
LF varsayıyordu); test okurken satır sonunu normalize edecek şekilde düzeltildi, yeniden 5/5.

## 3. Edge Function uçtan uca (G7)

**Ortam:** ana Postgres (A1'li / A1'siz) + PostgREST + GoTrue, müşteri Postgres (depodaki
`01` + `03` şema) + PostgREST, Edge Runtime, Node ağ geçitleri. Fonksiyonlar depodaki
kaynaktan, canlı adlarıyla. **Sürümler test ortamınındır; üretimle sürüm eşitliği iddia edilmez:**
postgres:17 `sha256:67f41722…`, PostgREST v12.2.3 `sha256:729bf65c…`, GoTrue v2.197.0
`sha256:1736a630…`, Edge Runtime v1.76.2 `sha256:edd22bef…`, supabase-js 2.116.0 (import map;
üretim kodu yüzen `@2` kullanır — bugün esm.sh bunu 2.116.0'a çözüyor). Tüm anahtarlar yerel JWT;
üretim sırrı veya verisi kullanılmadı.

- **hyper-api** (kod değişmedi): geçersiz/pasif token 400; 20 kalem/30 adet sınırı; `ODA_NO_GEREKLI`,
  `KONAKLAMA_YOK`, `FIYAT_DEGISTI` müşteriye döner ve arşivlenir; ücretli QR siparişi `bekliyor`
  başlar ve doğrulamadan hazırlanamaz.
- **rapid-handler** (kod değişti): oturumsuz/bozuk/anon/yabancı imzalı JWT 401; **pasif** ve bar
  yetkisiz kullanıcı 403; liste otel kapsamlı; başka otele ekleme 403, depo öneki uyuşmazlığı 400;
  başka otelin masası 404 (değişmedi). **Negatif kontrol:** kapsam filtresi kaldırılınca 811
  masaları 810 personeline görünüyor ve test bunu yakalıyor.
- **smooth-service** (regresyon): 401/403 kapıları ve yetkili yayın (`menu_yenile`) çalışıyor.

## 4. Yayın sırası — ölçülen ve önerilen

**Ölçülen:** (Y1) yeni menü istemcisi (`gosterilen_fiyat`) **bugünkü** veritabanıyla çalışıyor.
(Y2) yeni `rapid-handler` bugünkü veritabanıyla **çalışmıyor** (403) → migration'dan sonra deploy.
(H6) **eski** menü sayfası A1'den sonra ücretli QR siparişinde `FIYAT_DEGISTI` alır.

**Öneri (karar değil):** (1) yalnız `bar-menu.html` + `hata-kodlari.js` push — iki veritabanıyla da
çalışır; (2) yedek + T0/T1/T2 + A1 migration; (3) hemen kalan ekranlar push (garson, kuyruk,
stok-takip); (4) `rapid-handler` deploy (Dashboard, kullanıcı). (2)–(3) arası eski kuyrukta
doğrulama düğmesi yoktur ve hazırlanmış sipariş iptali hata verir; pencere kısa tutulmalı.
**Ölçülmeyen:** eski garson/kuyruk/stok-takip ekranlarının A1'li veritabanıyla ara dönem
davranışı ve eski `rapid-handler`'ın A1'li veritabanıyla çalışması (servis rolüyle tablo okur;
A1 bu tablolara dokunmaz, ama koşulmadı).

## 5. Bulgular

1. **Sayım tabloları üretimde oturum açmış kullanıcıya kapalı (A1 dışı, yüksek):** 09-13 dökümünde
   `sayim_oturumlari` yalnız RESTRICTIVE politika, `sayim_detaylari` hiç politika taşıyor (RLS açık).
   Ekran sayım oluşturamaz/listeleyemez. Test S0 bunu ölçüyor; ekran testleri için yalnız test
   ortamına etiketli okuma politikası eklendi. A1'e düzeltme **eklenmedi**; ayrı iş kaydedildi.
   Canlı durum salt-okuma sorgusuyla doğrulanmalı.
2. **Müşteri projesi şeması depodan ileride:** `masa_tokenlari.bolge` fonksiyonlarda kullanılıyor,
   depodaki şema dosyalarında yok (testte eklendi). Canlı müşteri şemasının dökümü yok.
3. **Canlı Edge kodu ≠ depo kodu olabilir:** deploy'lar Dashboard'dan yapılmış; canlı
   `hyper-api`/`smooth-service` gövdesinin depodakiyle aynı olduğu doğrulanmadı.
4. **Geri alma denetleyicide kırmızıydı (düzeltildi):** üretici pg_dump yetki satırlarını
   standart biçime çevirmiyordu; varsayılan denetleyici çalıştırması bu yüzden başarısızdı.
   Artık `GRANT EXECUTE`, `REVOKE … FROM PUBLIC, anon`, `SET search_path =`; etkin yetkinin
   değişmediği 4b2 ile ölçüldü, N4 negatif kontrolüyle testin farkı yakaladığı gösterildi.
5. **Stok-takip'te iki kapı:** ekran `rol==='cost_control'` istiyor, RPC `stok_takip≥kayit`.
   Ekran kapısı dokunulmadı (daha dar); yetki modeline taşınması ayrı karar.
6. Önceki rapordan süren: `rapid-handler` e-posta öneki/pasif/otel kapsamı açığı (A1 ile kapanıyor),
   bayat müşteri menüsünde `FIYAT_DEGISTI` döngüsü (mesajdaki fiyat+kimlikle giderildi), denetleyicinin
   `SET … TO` kör noktası, transfer önbellek ve kodsuz mal kabul işleri (ayrı kayıtlı).

## 5b. Gerçek tarayıcı kabul turu (2026-09-20 eklendi)

Ekran testleri Node'da sahte DOM ile koşuyor ve yerel `confirm/prompt` pencereleri test için
değiştiriliyordu; bu yüzden "koşullu geçti" sayılmışlardı. 2026-09-20'de izole ortamda
(`node scripts/bar-edge-e2e/tarayici-ortami.mjs`, `http://127.0.0.1:3300`) **gerçek Chrome'da,
gerçek pencerelerle ve kullanıcının tıklamasıyla** bir kabul turu yapıldı. Ekranlarda test için
hiçbir değişiklik yapılmadı; her adımın sonucu veritabanından okundu.

| Akış | Pencere | Sunucuda okunan sonuç |
|---|---|---|
| Ücretli siparişte **Doğrula** | `confirm` (kimlik/oda kartı beyanı) | `dogrulandi` + **dogrulayan** ve beyan metni kayıtlı |
| **Reddet** | `prompt` (ret nedeni) | sipariş `iptal`/`reddedildi`, neden kayıtlı |
| Kapalı folyoda **Teslim Et** — yetkisiz | `confirm` ("folyo KAPALI… yalnız yetkili personel") | **hiçbir şey yazılmadı**: sipariş `hazir`, istisna 0 satır, stok sabit |
| Kapalı folyoda **Teslim Et** — yetkili (şef) | aynı `confirm` | `istisna_bekliyor`, istisna kaydı açık (beyan veren = şef), viski 10 → 9, **folyoya borç yazılmadı** |
| Sayım **Onayla** | `confirm` | oturum `onaylandi`, `kismi_uygulandi = true`; Limon 80 → **70** |
| Bekleyen düzeltme **Uygula** | `confirm` | Bira 80 → **70**, detay `sonradan_uygulandi`, hareket kaydı doğru |

Karar 3'ün senaryosu (sistem 100 → sayılan 90 → arada 20 çıkış → onayda stok 80 → **70**) böylece
gerçek ekranda da doğrulandı. **Kapanmayan alt madde:** turda her pencerede "Tamam" seçildi;
"İptal" ve boş bırakma dalları gerçek tıklamayla ayrıca sınanmadı (otomatik ekran testlerinde var).
Ayrıntı: `2026-09-20-bar-a1-yayin-kapisi-raporu.md` §4.

## 5c. Kapsam düzeltmeleri (2026-09-20 kullanıcı uyarıları)

İki kabul ölçütü daraltıldı; ikisi de yalnız ifade değil, **kuralın kendisi** değişti.

**(a) Uzlaştırmada "KESİN" ölçütü — `belge_no` tek başına yetmez.** Kesinti uzlaştırması
(`docs/kurulum/2026-09-20-kesinti-uzlastirma.sql`, U2) önce "belge numarası dolu hareket var" ise
satırı KESİN sayıyordu. Artık **dört alanın birden** eşleşmesi gerekiyor: **belge/kalem + ürün +
depo + miktar**. Eşleştirilen kaynaklar: mal kabul kalemi (belge no + ürün + otel + miktar), bar
stok tüketimi (sipariş + ürün + depo + miktar), sayım bekleyen düzeltmesi (ürün + depo + fark).
Başka kaynaktan gelen hareket (elle giriş, harici düzeltme) KESİN sayılmaz.

| U2 sınıfı | Koşul | Prova |
|---|---|---|
| KESİN | dördü birden eşleşiyor | K2b, K2d |
| BELİRSİZ | belge numarası var, kalem/miktar eşleşmiyor | K2a (belge yok), K2c (miktar tutmuyor) |
| BELİRSİZ | hareket var, `belge_no` boş | K13 |
| BELİRSİZ | stok değişmiş, hareket yok | K1 |

`scripts/kesinti-uzlastirma.test.mjs` → **20/20**.

**(b) TRUNCATE kapsamı — A1 iki değil BEŞ tabloda kaldırıyor.** Önceki metinde "A1 yalnız iki
sayım tablosunu kapatıyor, 75 tablo kalıyor" denmişti; aradaki üç tablo eksik anlatılmıştı.

| Tablo | Neden | A1 bölümü |
|---|---|---|
| `sayim_oturumlari`, `sayim_detaylari` | 15c: politika eklemek yetmez, TRUNCATE RLS'i dinlemez | 15c |
| `bar_siparisleri`, `bar_siparis_kalemleri`, `stok_rezervasyonlari` | doğrudan yazmanın kapatılmasının yan etkisi | 17 |

Aritmetik tek sorgudan: **80 − 5 = 75** (`scripts/truncate-yetki.test.mjs` C3/C4/C5, **13/13**).
**Bu sayılar 2026-09-13 şema dökümünden kurulan izole kopyada ölçüldü; canlı bulgu değildir.**
Canlı doğrulama salt okuma dosyasıyla yapılacak; üretimde TRUNCATE denenmeyecek. Ayrıntı:
`2026-09-20-truncate-bulgusu.md`.

## 6. Test açıkları

- ~~Ekran testleri … gerçek tarayıcıda tıklama akışı denenmedi.~~ → **5b ile kapandı**; yalnız
  "İptal"/boş bırakma dalları gerçek tıklamayla sınanmadı.
- **Sayım oluşturma/listeleme/reddetme gerçek yetki düzeniyle doğrulanmadı — çünkü bugünkü şemada
  hiçbir ortamda çalışmıyor** (doğrudan tabloya gider, permissive politika yok). Ölçüm ve sonuçları:
  `2026-09-20-bar-a1-yayin-onerisi.md` §2. A1 dışı kusur; ayrı iş.
- **Canlı `rapid-handler` repodaki sürüm değil** (canlı `v=bolge1`, repo `v=anon3`). Deploy öncesi
  canlı kaynak indirilmeli: `2026-09-20-bar-a1-yayin-onerisi.md` §4.
- Ön büro istisna çözüm ekranı A1'de yok (yalnız RPC; DB B6–B7 ile test edildi).
- Bölüm 4'teki ara dönem davranışları ölçülmedi.
- Test sürümleri üretim sürümleri değil; GoTrue/Edge Runtime/PostgREST canlı sürümleri bilinmiyor.
