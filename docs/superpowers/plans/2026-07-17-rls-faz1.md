# RLS Faz 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 11 tabloda RLS açıp SELECT/INSERT/UPDATE'i serbest bırakarak, DELETE'i veritabanı seviyesinde tamamen engellemek — devtools'tan ham bir DELETE isteği bile artık işe yaramayacak.

**Architecture:** Tek bir SQL scripti (kullanıcı Supabase SQL editöründe çalıştırır), `begin`/`commit` ile atomik — 11 tabloya aynı 4 satırlık şablon uygulanır. Kod değişikliği yok. Doğrulama, Claude'un `curl` ile doğrudan bir DELETE isteği gönderip reddedildiğini teyit etmesiyle yapılır.

**Tech Stack:** Postgres RLS (Supabase). Kod tabanına dokunulmuyor.

## Global Constraints

- Her tabloda `SELECT`/`INSERT`/`UPDATE` politikaları `using(true)`/`with check(true)` ile TAMAMEN serbest bırakılır — bugünkü davranış birebir korunur, regresyon YOK.
- DELETE için hiçbir politika tanımlanmaz — Postgres RLS kuralı gereği bu, DELETE'i varsayılan olarak reddeder.
- `yevmiye_kalemleri`/`fatura_kalemleri`/`recete_kalemleri`/`siparis_kalemleri` gibi satır-kalemi tablolarına DOKUNULMAZ — onlar hâlâ sil-yeniden-yaz deseni kullanıyor, DELETE'e ihtiyaçları var.
- Tüm SQL tek bir `begin`/`commit` transaction'ı içinde — biri hata verirse hiçbiri uygulanmaz.
- Kapsam TAM OLARAK 11 tablo: `cariler`, `faturalar`, `demirbaslar`, `cek_senetler`, `banka_kasa_hesaplari`, `butce_kayitlari`, `kullanicilar`, `stok`, `stok_hareketleri`, `cari_hareketler`, `banka_kasa_hareketleri`.

---

### Task 1: Supabase RLS politikaları

**Files:** (yok — SQL, kullanıcı Supabase SQL editöründe çalıştırır)

- [ ] **Step 1: SQL'i kullanıcıya ver**

```sql
begin;

alter table cariler enable row level security;
create policy "allow_select" on cariler for select using (true);
create policy "allow_insert" on cariler for insert with check (true);
create policy "allow_update" on cariler for update using (true) with check (true);

alter table faturalar enable row level security;
create policy "allow_select" on faturalar for select using (true);
create policy "allow_insert" on faturalar for insert with check (true);
create policy "allow_update" on faturalar for update using (true) with check (true);

alter table demirbaslar enable row level security;
create policy "allow_select" on demirbaslar for select using (true);
create policy "allow_insert" on demirbaslar for insert with check (true);
create policy "allow_update" on demirbaslar for update using (true) with check (true);

alter table cek_senetler enable row level security;
create policy "allow_select" on cek_senetler for select using (true);
create policy "allow_insert" on cek_senetler for insert with check (true);
create policy "allow_update" on cek_senetler for update using (true) with check (true);

alter table banka_kasa_hesaplari enable row level security;
create policy "allow_select" on banka_kasa_hesaplari for select using (true);
create policy "allow_insert" on banka_kasa_hesaplari for insert with check (true);
create policy "allow_update" on banka_kasa_hesaplari for update using (true) with check (true);

alter table butce_kayitlari enable row level security;
create policy "allow_select" on butce_kayitlari for select using (true);
create policy "allow_insert" on butce_kayitlari for insert with check (true);
create policy "allow_update" on butce_kayitlari for update using (true) with check (true);

alter table kullanicilar enable row level security;
create policy "allow_select" on kullanicilar for select using (true);
create policy "allow_insert" on kullanicilar for insert with check (true);
create policy "allow_update" on kullanicilar for update using (true) with check (true);

alter table stok enable row level security;
create policy "allow_select" on stok for select using (true);
create policy "allow_insert" on stok for insert with check (true);
create policy "allow_update" on stok for update using (true) with check (true);

alter table stok_hareketleri enable row level security;
create policy "allow_select" on stok_hareketleri for select using (true);
create policy "allow_insert" on stok_hareketleri for insert with check (true);
create policy "allow_update" on stok_hareketleri for update using (true) with check (true);

alter table cari_hareketler enable row level security;
create policy "allow_select" on cari_hareketler for select using (true);
create policy "allow_insert" on cari_hareketler for insert with check (true);
create policy "allow_update" on cari_hareketler for update using (true) with check (true);

alter table banka_kasa_hareketleri enable row level security;
create policy "allow_select" on banka_kasa_hareketleri for select using (true);
create policy "allow_insert" on banka_kasa_hareketleri for insert with check (true);
create policy "allow_update" on banka_kasa_hareketleri for update using (true) with check (true);

commit;
```

- [ ] **Step 2: Kullanıcıdan onay al**

"Çalıştı" onayını bekle. Onaysız Task 2'ye geçme. Eğer bir hata alırsa (örn. bir politika zaten varsa `already exists` hatası), hata mesajını olduğu gibi iste — muhtemelen ilgili tabloyu script'ten çıkarıp tekrar denemek gerekir.

---

### Task 2: Uçtan uca doğrulama

**Files:** (yok — sadece doğrulama)

- [ ] **Step 1: DELETE'in reddedildiğini doğrudan test et**

SQL çalıştıktan hemen sonra, `stok_hareketleri` tablosundan var olmayan bir `id` için bir DELETE isteği gönder (gerçek veri riske atılmaz, id zaten yok — sadece RLS'in isteği daha veriye bakmadan reddettiğini görmek için):

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X DELETE \
  "$SB_URL/rest/v1/stok_hareketleri?id=eq.00000000-0000-0000-0000-000000000000" \
  -H "apikey: $SB_KEY" -H "Authorization: Bearer $SB_KEY"
```

(`$SB_URL`/`$SB_KEY` için `stok-takip.html`'deki `SB_URL`/`SB_KEY` sabit değerlerini kullan.)

Expected: `403` (RLS politika ihlali — `42501`). `200`/`204` dönerse RLS beklendiği gibi çalışmıyor demektir, hemen durup kullanıcıya bildir.

- [ ] **Step 2: SELECT/INSERT/UPDATE'in hâlâ çalıştığını doğrula**

```bash
curl -s -o /dev/null -w "%{http_code}\n" \
  "$SB_URL/rest/v1/cariler?select=id&limit=1" \
  -H "apikey: $SB_KEY" -H "Authorization: Bearer $SB_KEY"
```

Expected: `200`. Bu, RLS'in okumaları bozmadığını doğrular.

- [ ] **Step 3: Kullanıcıya manuel test adımlarını bildir**

Node/Python yok. Kullanıcının tarayıcıda doğrulaması gereken akış:
1. `muhasebe-cariler.html`'de yeni bir cari oluştur, düzenle → eskisi gibi çalıştığını doğrula.
2. `muhasebe-butce.html`'de bir bütçe kaydı oluştur, düzenle, sil (soft-delete → listeden kaybolmalı) → eskisi gibi çalıştığını doğrula.
3. `stok-takip.html`'de bir stok girişi/çıkışı yap (RPC üzerinden) → eskisi gibi çalıştığını doğrula (RLS'in `stok`'ta UPDATE'i engellemediğini teyit eder).
4. Herhangi bir hata/kırılma görülürse hemen bildir — RLS politikalarının gözden geçirilmesi gerekir.

- [ ] **Step 4: Kullanıcıdan onay al**

Bu iş kod değiştirmediği için push gerektirmez — sadece kullanıcının "test ettim, çalışıyor" onayını al ve işi tamamlanmış say.
